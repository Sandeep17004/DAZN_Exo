package com.example.dazn_exoplayerproject.repository

import com.example.dazn_exoplayerproject.model.ExoPlayerActionEvents

class FireBaseRepositoryImpl : FireBaseRepository {
    override fun sendEvent(exoPlayerActionEvents: ExoPlayerActionEvents) {
        // 1. inject the required configuration classes
        // 2. Send events from here
    }
}